import TransitSupply from '@/visualization/TransitSupply.vue'

describe('TransitSupply', () => {
  it('should be instanceable', () => {
    expect(new TransitSupply()).toBeInstanceOf(TransitSupply)
  })
})
