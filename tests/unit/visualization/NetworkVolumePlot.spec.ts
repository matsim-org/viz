import NetworkVolumePlot from '@/visualization/NetworkVolumePlot.vue'

describe('NetworkVolumePlot', () => {
  it('should be instanceable', () => {
    expect(new NetworkVolumePlot()).toBeInstanceOf(NetworkVolumePlot)
  })
})
